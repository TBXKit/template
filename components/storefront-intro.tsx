/**
 * Starter homepage content — not generated, not Tebex data, just plain JSX
 * meant to be rewritten. Ships intentionally unfinished (a bracketed
 * placeholder, not polished copy) so it reads as "edit me," not as
 * finished content a forker could mistake for real and leave in place.
 * Renders between the Hero and the live category grid — see app/page.tsx.
 */
export function StorefrontIntro({ storeName }: { storeName: string }) {
  return (
    <p className="max-w-2xl text-foreground">
      {/* Replace this paragraph with real homepage copy. */}
      {storeName} sells [what you sell]. Add a real intro here.
    </p>
  );
}
